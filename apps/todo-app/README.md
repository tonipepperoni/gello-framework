# todo-app

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build todo-app` to build the library.

## Running unit tests

Run `nx test todo-app` to execute the unit tests via [Vitest](https://vitest.dev/).
