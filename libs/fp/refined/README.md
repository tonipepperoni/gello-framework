# refined

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build refined` to build the library.

## Running unit tests

Run `nx test refined` to execute the unit tests via [Vitest](https://vitest.dev/).
