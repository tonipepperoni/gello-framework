{
  "name": "gello-queue",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/publishable/queue/src",
  "projectType": "library",
  "tags": ["scope:public", "type:lib"],
  "targets": {
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "angular",
        "tagPrefix": "queue-v",
        "postTargets": ["gello-queue:publish"]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "npm publish --access public",
        "cwd": "libs/publishable/queue"
      }
    }
  }
}
